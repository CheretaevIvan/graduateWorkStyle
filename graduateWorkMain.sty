	\RequirePackage{ifpdf} 	% для раздельной компиляции pdf и dvi 
	
	\newcommand{\STYLES}{\globalStylesDir}	
	\newcommand{\IMG}{\globalPictureDir}
	\newcommand{\SRC}{\globalSourceDir}
	
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%% жизненноважная директива
%%%
	
	\usepackage[\globalEncoding]{inputenc}							% кодировка, тут очень аккуратно
		
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%% опишем модули, раздельное описание --- легче редактировать
%%%

	\usepackage{\STYLES/graduateWorkTitle}       	% титульная страница
	\usepackage{\STYLES/graduateWorkPage}			% параметры страницы
	\usepackage{\STYLES/graduateWorkFonts}			% шрифты и локализация
	\usepackage{\STYLES/graduateWorkMath}			% математика 
	\usepackage{\STYLES/graduateWorkGraphics}		% графика
	\usepackage{\STYLES/graduateWorkCode}			% листинги кода
	\usepackage{\STYLES/graduateWorkTables} 		% таблицы
	\usepackage{\STYLES/graduateWorkColRows}		% многополосность
	\usepackage{\STYLES/graduateWorkQuotes}			% цитаты
	\usepackage{\STYLES/graduateWorkTheorem}		% теоремы и определения
	\usepackage{\STYLES/graduateWorkAlgorithms}		% алгоритмы	
	\usepackage{\STYLES/graduateWorkHyperref}		% гиперссылки
	\usepackage{\STYLES/graduateWorkAppendix}    	% приложения
%	
%		
%	%% это уже косметика, но их вынесем отдельно
	\usepackage{\STYLES/graduateWorkLists} 		% списки (переопределение буллетов)
	\usepackage{\STYLES/graduateWorkSections} 		% секции (настройка отображения частей)
